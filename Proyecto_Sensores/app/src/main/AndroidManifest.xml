<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_sensores">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_Sensores">
        <activity android:name=".modificarUsuario"></activity>
        <activity android:name=".altaUsuario" />
        <activity android:name=".inicioSecion" />
        <activity
            android:name=".pantallaPrincipalMenuLateral"
            android:label="@string/title_activity_pantalla_principal_menu_lateral"
            android:theme="@style/Theme.Proyecto_Sensores.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>